name: Discord Release Notification

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  notify-discord:
    runs-on: ubuntu-latest
    steps:
      - name: Send release notification to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          # Handle both release event and manual trigger
          if [ -n "${{ github.event.release.tag_name }}" ]; then
            VERSION="${{ github.event.release.tag_name }}"
            RELEASE_NAME="${{ github.event.release.name }}"
            CHANGELOG="${{ github.event.release.body }}"
            RELEASE_URL="${{ github.event.release.html_url }}"
            AUTHOR="${{ github.event.release.author.login }}"
          else
            # Fallback for manual trigger - use latest release
            LATEST_RELEASE=$(gh release view --repo ${{ github.repository }} --json tagName,name,body,htmlUrl,author.login)
            VERSION=$(echo "$LATEST_RELEASE" | jq -r '.tagName')
            RELEASE_NAME=$(echo "$LATEST_RELEASE" | jq -r '.name')
            CHANGELOG=$(echo "$LATEST_RELEASE" | jq -r '.body')
            RELEASE_URL=$(echo "$LATEST_RELEASE" | jq -r '.htmlUrl')
            AUTHOR=$(echo "$LATEST_RELEASE" | jq -r '.author.login')
          fi
          REPO_NAME="${{ github.repository }}"
          TIMESTAMP=$(date -u +%Y-%m-%dT%H:%M:%SZ)
          
          # Truncate changelog if too long (Discord embed limit is 2000 chars)
          if [ ${#CHANGELOG} -gt 2000 ]; then
            CHANGELOG="${CHANGELOG:0:1997}..."
          fi
          
          # Escape JSON special characters
          CHANGELOG_ESCAPED=$(echo "$CHANGELOG" | sed 's/\\/\\\\/g' | sed 's/"/\\"/g' | sed ':a;N;$!ba;s/\n/\\n/g')
          RELEASE_NAME_ESCAPED=$(echo "$RELEASE_NAME" | sed 's/\\/\\\\/g' | sed 's/"/\\"/g')
          
          # Create and send JSON payload using jq for proper JSON handling
          curl -X POST "$DISCORD_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d "{
              \"embeds\": [{
                \"title\": \"🚀 New Release: $RELEASE_NAME_ESCAPED\",
                \"description\": \"$CHANGELOG_ESCAPED\",
                \"url\": \"$RELEASE_URL\",
                \"color\": 5865,
                \"fields\": [
                  {
                    \"name\": \"Version\",
                    \"value\": \"$VERSION\",
                    \"inline\": true
                  },
                  {
                    \"name\": \"Repository\",
                    \"value\": \"[$REPO_NAME](https://github.com/$REPO_NAME)\",
                    \"inline\": true
                  }
                ],
                \"author\": {
                  \"name\": \"$AUTHOR\",
                  \"url\": \"https://github.com/$AUTHOR\",
                  \"icon_url\": \"https://github.com/$AUTHOR.png\"
                },
                \"footer\": {
                  \"text\": \"GitHub Release\",
                  \"icon_url\": \"https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png\"
                },
                \"timestamp\": \"$TIMESTAMP\"
              }]
            }"


