[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["pip install discord.py>=2.3.0", "pip install mcp>=0.1.0"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "python railway_bot.py"

